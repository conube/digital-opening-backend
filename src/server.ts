import { app } from './app'
import { GlobalSettings } from '@/settings/global.settings'

// class Server {
//     private _port: number
//     private _app: any

//     constructor(app: any, port) {
//         this._port = port
//         this._app = app
//         this.init()
//     }

//     public init() {
//         this._app.listen(this._port)
//     }
// }

// export default new Server(app, GlobalSettings.HTTP_PORT)